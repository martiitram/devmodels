apiVersion: v1
kind: Service
metadata:
  name: java-springboot-ci-cd-with-github-actions
spec:
  selector:
    app: java-springboot-ci-cd-with-github-actions
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080